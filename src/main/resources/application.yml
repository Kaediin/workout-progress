server:
 port: 5000
spring:
 data:
  mongodb:
   uri: ${MONGO_URI:mongodb://localhost:27017/nonexistent}
   database: test
   auto-index-creation: true
cors:
 allowed_origins: ${CORS_ALLOWED_ORIGINS:http://localhost}
whitelist:
 allowed_client_id: ${WHITELIST_ALLOWED_CLIENT_ID:}
 allowed_client_secret: ${WHITELIST_ALLOWED_CLIENT_SECRET:}
 allowed_remotes: ${WHITELIST_ALLOWED_REMOTES:}
aws:
 cognito:
  cognitoId: ${AWS_COGNITO_ID:}
  cognitoKey: ${AWS_COGNITO_KEY:}
  userPoolId: ${AWS_COGNITO_USER_POOL_ID:}
  region: ${AWS_COGNITO_REGION:eu-central-1}
graphql:
 servlet:
  enabled: true
  mapping: /graphql
  #  actuator-metrics: true
  corsEnabled: true
  cors:
   allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost}
  exception-handlers-enabled: true
  contextSetting: PER_REQUEST_WITH_INSTRUMENTATION
 tools:
  schema-location-pattern: "/graphql/*.graphqls"
graphiql:
 enabled: ${GRAPHIQL_ENABLED:false}
 mapping: /graphiql
 endpoint:
  graphql: /graphql
#tasks:
# FetchWorkoutsTask:
#  cron: '${FETCHWORKOUTS_TASK_CRON:0 0 0 0 * ?}'
